version: '3'

tasks:
  bandwhich:
    desc: >
      WARNING Needs sudo

      [bandwhich - GitHub](https://github.com/imsnif/bandwhich):
      Terminal bandwidth utilization tool.
    cmd: cargo install --force --root ~/.local bandwhich
    preconditions:
      - msg: bandwhich is already present
        sh: "! which bandwhich"

  grex:
    desc: >
      [grex - GitHub](https://github.com/pemistahl/grex):
      command-line tool and Rust library for generating regular expressions
      from user-provided test cases.
      [Generate a matching regular expression from the test cases you provide](https://pemistahl.github.io/grex-js/)
    cmd: cargo install --force --root ~/.local grex
    preconditions:
      - msg: grex is already present
        sh: "! which grex"

  atuin:
    desc: >
      [atuin](https://github.com/atuinsh/atuin):
      ✨ Magical shell history.
    cmd: cargo install --force --root ~/.local atuin
    preconditions:
      - msg: atuin is already present
        sh: "! which atuin"
